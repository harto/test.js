/*
 * A sample library of functions requiring unit testing.
 */

function frobnicate(foo) {
    if (foo === 'bar') {
        return 'baz';
    } else if (foo === 42) {
        return null;
    } else if (foo === 'quux') {
        return 99;
    }
}
